import { Component } from '@angular/core';

var UndoRedoComponent = Component({
    selector: 'undo-redo',
    inputs : ['hot'], 
    templateUrl: 'templates/undoRedo.html',
})
.Class({
    constructor: function() {
    },
});

export default UndoRedoComponent;